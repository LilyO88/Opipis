<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/header_height"
    android:gravity="bottom"
    android:orientation="vertical"
    android:padding="@dimen/header_left_padding"
    android:theme="@style/ThemeOverlay.AppCompat.Dark"
    android:background="@drawable/fondo"
    android:id="@+id/ll_drawer_header">

<!--    <ImageView
        android:id="@+id/img_drawer_header"
        android:layout_width="@dimen/perfil_image_size"
        android:layout_height="@dimen/perfil_image_size"
        android:layout_marginBottom="@dimen/margin_16"
        android:src="@drawable/ic_school_black_64dp"/>-->

    <TextView
        android:id="@+id/title_drawer_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/title_drawer_header"
        android:textAppearance="@style/TextAppearance.AppCompat.Body1"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:textSize="40dp"/>

</LinearLayout>